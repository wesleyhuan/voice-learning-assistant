# Voice Learning Assistant — Requirements
# Tested on Python 3.10 / 3.11

# ── Frontend ──────────────────────────────────────────────────────────────
streamlit>=1.31.0          # st.audio_input() requires 1.31+

# ── LLM ───────────────────────────────────────────────────────────────────
anthropic>=0.28.0
python-dotenv>=1.0.0       # load ANTHROPIC_API_KEY from .env

# ── RAG Pipeline ──────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0   # RecursiveCharacterTextSplitter (split out of langchain in 1.x)
langchain-core>=0.2.0             # Document, base abstractions
chromadb>=0.4.0
# Keep chromadb's OpenTelemetry exporter in sync with its otel-api/sdk.
# A stale exporter ships old protobuf stubs and crashes under protobuf 7.x
# ("Descriptors cannot be created directly"); this pin also caps protobuf.
opentelemetry-exporter-otlp-proto-grpc>=1.42.0
sentence-transformers>=2.7.0
rank-bm25>=0.2.2
flashrank>=0.2.0

# ── Voice ─────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0
edge-tts>=6.1.9

# ── Document Ingestion ────────────────────────────────────────────────────
PyMuPDF>=1.24.0
trafilatura>=1.9.0
